# Prototype Run Brief

- `brief_id`: brief-fl-n4-2
- `status`: final
- `active_region_ref`: N4
- `parent_seed_ref`: seed-v01-final.md
- `run_mode`: changed_slice
- `iteration_number`: 2
- `checkpoint`: 材料导入完成、第一次追问之前
- `restored_preconditions`: 已导入两组概念型材料，用户未作答
- `changed_slice`: N4 追问链：确定目标层后按回答补一个变量
- `evidence_goal`: 回答是否暴露原本看不到的误解，追问是否在证据满足后停止
- `completion_criteria`: 两组材料各完成一次追问链，并记录停止点
- `appetite`: 2
- `coverage_limit`: 只覆盖 N4 追问链；不代表完整路径的整合结果

## 说明

路径字段写相对于本文件的路径，或绝对路径。
